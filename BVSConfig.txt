# This is an example config file.
#
# For more information:
# TOPIC        FILE                              DOXYGEN
# syntax       'libbvs/include/bvs/config.h'     'Classes->BVS::Config'
# bvs options  'libbvs/include/bvs/bvs.h'        'Classes->BVS::BVS'
# bvs defaults 'libbvs/include/bvs/traits.h'     'Files->include/bvs/traits.h'

# Settings for the BVS framework.
[BVS]
# warn about connector type mismatch
connectorTypeMatching = ON

# enable logging system
logSystem = ON

# log to console
logConsole = ON

# log to file (empty disables, +FILE appends)
#logFile = BVSLog.txt

# overall system log verbosity
logVerbosity = 1

# allow modules to run in dedicated threads
moduleThreads = ON

# force modules to run in dedicated threads
forceModuleThreads = OFF

# list of modules to load
# format: uniqueID(moduleLibrary)
# this allows to load modules more than once
# format: uniqueID(moduleLibrary).input(test.output)
# this connects uniqueID's Connector named 'input' with 'test' module's
# connector named 'output'
modules = +capture(bvsCapture)
+modules = +calibration(bvsCalibration).input0(capture.outL).input1(capture.outR)
#modules = calibration(bvsCalibration)
#+modules = +id2(library2).input(id.output)
#+modules = ...



# Logger level settings.
[Logger]
#BVS = 0
#BVSDaemon = 0
#BVSMaster = 0
#YourLogger = ...

[calibration]
numNodes = 2
numImages = 100
circleSize = 2.42
autoShotMode = ON
autoShotDelay = 1
saveImages = ON
useSavedImages = OFF
loadCalibration = ON
saveCalibration = ON
directory = "calibrationData"
calibrationFile = "stereoCalibration.yml"
createRectifiedOutput = ON
addGridOverlay = ON
useCalibrationGuide = ON
centerScale = 0.5
centerDetections = 60
sectorDetections = 5

# This line tells vim something about the file type. -- vim:ft=config:
